---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodetemplatesets.sa.gardener.cloud
spec:
  group: sa.gardener.cloud
  names:
    kind: NodeTemplateSet
    listKind: NodeTemplateSetList
    plural: nodetemplatesets
    shortNames:
    - nts
    singular: nodetemplateset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NodeTemplateSet defines top-level object with a [NodeTemplateSetSpec] holding all the [NodeTemplate]'s known
          to the scaling-advisor. Any NodeTemplate referenced in a [NodePool] within a [ScalingConstraintSpec] must have a
          corresponding [NodeTemplate] object within the data plane.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the specification of the NodeTemplateSet
            properties:
              templates:
                description: Templates is a slice of [NodeTemplate]'s. NodeTemplate
                  names should be unique.
                items:
                  description: |-
                    NodeTemplate defines a node template configuration for an instance type. Multiple NodeTemplates may reference the
                    same instance type which permits different reserved resource configurations for the same underlying instance type.
                    However, node template names are expected to be unique within a [NodeTemplateSet].
                  properties:
                    architecture:
                      description: Architecture is the architecture of the instance
                        type.
                      minLength: 1
                      type: string
                    capacity:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: Capacity specifies the capacity of resources that
                        are available for the instance type of this node template.
                      type: object
                    instanceType:
                      description: InstanceType is the instance type of the node template.
                      minLength: 1
                      type: string
                    maxVolumes:
                      description: MaxVolumes is the max number of volumes that can
                        be attached to a node of this instance type.
                      format: int32
                      minimum: 0
                      type: integer
                    name:
                      description: Name is a logical name given to this NodeTemplate
                        specification
                      minLength: 1
                      type: string
                    reservedCapacity:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: Reserved specifies the capacity for total reserved
                        resources. This should include both system and Kubernetes
                        components.
                      type: object
                  required:
                  - architecture
                  - capacity
                  - instanceType
                  - maxVolumes
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - templates
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
