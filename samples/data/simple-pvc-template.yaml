apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{.Name}}"
  namespace: "{{.Namespace}}"
spec:
  accessModes:
   - "{{.AccessMode}}"
  resources:
    requests:
      storage: "{{.Storage}}"
