apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{.Name}}"
  namespace: "{{.Namespace}}"
spec:
  storageClassName: "{{.StorageClassName}}"
  accessModes:
   - "{{.AccessMode}}"
  resources:
    requests:
      storage: "{{.Storage}}"
status:
  phase: "Bound"